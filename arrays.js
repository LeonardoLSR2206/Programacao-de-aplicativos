let times = ['Coritiba', 'Athletico', 'Maringá', 'Operário', 'Londrina'];
// times[1] = 'Rio Branco' // Substitui o elemento do array por posição
// times.push('Paraná'); // Insere elemento no array
// times.pop(); // Remove último elemento
// times.unshift('Cascavel'); // Insere na primeira posição
// times.shift(); // Remove da primeira posição
// console.log(times.length); // Verifica o tamanho do array
console.log(times.sort());
console.log(times);
console.log(times.reverse());
console.log(times);

// console.log(times.indexOf('Maringá'));

// console.log(times.includes('Coritiba') || times.includes('Flamengo'));

// console.log(times);
